---
title: Get a booking by project
description: A guide to retrieving bookings for a specific project using the Fstop API
---

import { Steps } from '@astrojs/starlight/components';
import { Code } from 'astro-expressive-code/components';
import get_bookings_by_project_curl from '/src/snippets/get_requests/get_bookings_by_project_curl.sh?raw';
import get_bookings_by_project_response from '/src/snippets/responses/get_bookings_by_project_response.json?raw';
import get_bookings_by_project_sdk from '/src/snippets/sdk/get_bookings_by_project_sdk.py?raw';

The Fstop API allows you to retrieve bookings filtered by a specific project using a query parameter.

In this guide, you'll make a `GET` request to the `/api/bookings/` endpoint with a `project_id` query parameter to retrieve all bookings for a specific project.

## Before you begin

Before you make a `GET` request, ensure that you have an access token. For instructions on requesting an access token, see [Getting started with the Fstop API](./getting-started).

You'll also need the UUID of the project whose bookings you want to retrieve.

## Make a GET request with a query parameter

<Steps>
1. Open a terminal and run the following command:
   <Code code={get_bookings_by_project_curl} lang="shellscript" title="get_bookings_by_project.sh" />
   
   The API returns a `200 OK` and a list of bookings for that project:
   <Code code={get_bookings_by_project_response} lang="json" title="get_bookings_by_project_response.json" />

2. Using the Python SDK:
   <Code code={get_bookings_by_project_sdk} lang="python" title="get_bookings_by_project.py" />

</Steps>

You can use the filtered booking data in your application. For example, you could display a timeline of all bookings for a project or check availability before scheduling a new booking.

## What to do next

See [GET /api/bookings/](/api-reference) for a full list of request parameters and response schemas.