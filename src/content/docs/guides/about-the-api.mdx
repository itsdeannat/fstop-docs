---
title: About the API
description: Information about the Fstop RESTful API
---

The Fstop API is a RESTful API built with the Django REST Framework. It accepts JSON in API requests and returns JSON in all responses. Currently, the API supports `GET` and `POST` requests from clients.